{
  "name": "collabroom-selenium-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --runInBand --forceExit --json --outputFile=jest-results.json",
    "test:report": "npm test && node ../reports/generate-reports.js"
  },
  "dependencies": {
    "selenium-webdriver": "^4.18.1",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "exceljs": "^4.4.0",
    "dotenv": "^16.0.0"
  },
  "jest": {
    "testTimeout": 120000,
    "reporters": [
      "default",
      "jest-junit"
    ]
  }
}
